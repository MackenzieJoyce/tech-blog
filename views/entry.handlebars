<div class="card">
  <div class="card-header">
    <h3>{{post_subject}}</h3>
    <span>Posted by {{post_author}} on {{posted_date}}</span>
  </div>
  <div class="card-body">
    <p class="card-text">{{post_entry}}</p>
  </div>
</div>